<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add Interception &amp; Modification Rule:"
        android:textColor="@color/text_primary"
        android:textSize="13sp"
        android:textStyle="bold" />

    <Spinner
        android:id="@+id/spinnerRuleType"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="6dp" />

    <EditText
        android:id="@+id/etRulePattern"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="6dp"
        android:background="@drawable/search_bg"
        android:hint="Domain / URL Pattern (e.g. *.api.com)"
        android:paddingHorizontal="10dp"
        android:textSize="12sp" />

    <EditText
        android:id="@+id/etRuleAction"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginTop="6dp"
        android:background="@drawable/search_bg"
        android:hint="Action (Redirect URL or Mock JSON Body)"
        android:paddingHorizontal="10dp"
        android:textSize="12sp" />

    <Button
        android:id="@+id/btnAddRule"
        android:layout_width="match_parent"
        android:layout_height="38dp"
        android:layout_marginTop="8dp"
        android:text="+ Add Rule"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/tvActiveRulesList"
        android:layout_width="match_parent"
        android:layout_height="180dp"
        android:layout_marginTop="12dp"
        android:background="@drawable/status_chip_bg"
        android:fontFamily="monospace"
        android:padding="8dp"
        android:scrollbars="vertical"
        android:textColor="@color/text_primary"
        android:textSize="11sp" />

</LinearLayout>
